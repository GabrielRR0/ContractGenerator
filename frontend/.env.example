# URL del backend desplegado en produccion. Sin definir, el frontend usa
# rutas relativas (funciona en dev via el proxy de vite.config.ts).
# Ej: https://contract-generator-api.onrender.com
VITE_API_BASE_URL=
